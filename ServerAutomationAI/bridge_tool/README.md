# 🌉 Bridge Tool

أداة CLI متكاملة للربط بين بيئة التطوير على Replit والسيرفر الإنتاجي **عبر GitHub**.

## ✨ المزايا الجديدة

- 🔄 **النشر عبر GitHub**: Replit → GitHub → Server
- 🏷️ **إدارة الإصدارات بـ Git Tags**: تتبع كامل للإصدارات
- ⏪ **التراجع السهل**: العودة لأي إصدار سابق بأمر واحد
- 📋 **تقارير مفصلة**: سجل كامل لكل عملية نشر
- 🔒 **أمان محسّن**: استخدام GitHub Token من Replit Secrets

## 🚀 البداية السريعة

### 1. إعداد الأسرار (Secrets)

في Replit Secrets، أضف:
- `GITHUB_TOKEN`: Personal Access Token من GitHub
- `GITHUB_REPO`: اسم المستودع بصيغة `username/repo-name`
- `SSH_HOST`: عنوان IP للسيرفر
- `SSH_PORT`: منفذ SSH (عادة 22)
- `SSH_USER`: اسم المستخدم على السيرفر
- `SSH_PASSWORD`: كلمة المرور (أو استخدم SSH Key)

### 2. التهيئة والنشر

```bash
# 1. التهيئة (إذا لم تكن قد فعلت)
python3 bridge_tool/cli.py init

# 2. تحرير الإعدادات (اختياري)
nano bridge.config.yaml

# 3. اختبار الاتصال
python3 bridge_tool/cli.py test

# 4. النشر التجريبي (بدون تغييرات فعلية)
python3 bridge_tool/cli.py push --dry-run

# 5. النشر الفعلي
python3 bridge_tool/cli.py push
```

## 📋 الأوامر

| الأمر | الوصف |
|-------|-------|
| `init` | إنشاء ملف الإعداد |
| `push` | نشر عبر GitHub ثم إلى السيرفر (Replit → GitHub → Server) |
| `push --dry-run` | اختبار النشر بدون تغييرات فعلية |
| `pull <what>` | جلب logs/backups/configs من السيرفر |
| `status` | فحص حالة النظام على السيرفر |
| `exec <cmd>` | تنفيذ أمر على السيرفر |
| `rollback` | التراجع للإصدار السابق |
| `rollback --list` | عرض قائمة الإصدارات المتاحة |
| `rollback -r <tag>` | التراجع لإصدار محدد |
| `test` | اختبار الاتصال بـ GitHub والسيرفر |

## 📖 كيف يعمل النشر عبر GitHub؟

### المسار الكامل:

```
1. Replit (التطوير)
   ├─ التغييرات في الكود
   ├─ Auto-commit (تلقائي)
   └─ Create Git Tag (release_YYYYMMDD_HHMMSS)
   
2. ⬇️ Push to GitHub
   ├─ رفع الكود
   └─ رفع الـ Tag
   
3. ⬇️ Deploy to Server
   ├─ الاتصال بالسيرفر عبر SSH
   ├─ سحب الكود من GitHub (git clone/pull)
   ├─ Checkout إلى الـ Tag المحدد
   ├─ تشغيل سكربتات ما بعد النشر
   └─ تحديث الرابط الرمزي (current)
```

### المزايا:

✅ **نسخة احتياطية تلقائية**: كل نشر مسجل في GitHub  
✅ **تتبع التغييرات**: تاريخ كامل للتعديلات  
✅ **سهولة التراجع**: العودة لأي إصدار بسهولة  
✅ **استقلالية**: لا حاجة للاتصال المباشر بـ Replit للنشر  
✅ **أمان**: استخدام SSH Keys و GitHub Tokens

## 📚 الوثائق

- [دليل الاستخدام الكامل](../docs/BRIDGE_TOOL_GUIDE.md)
- [قائمة التحقق للنشر](../docs/DEPLOY_CHECKLIST.md)
- [متطلبات السيرفر](../docs/SERVER_REQUIREMENTS.md)

## ⚙️ المتطلبات

```bash
pip install paramiko pyyaml
```

## 🔒 الأمان

- استخدم SSH keys بدلاً من كلمات المرور
- احفظ المعلومات الحساسة في Replit Secrets
- راجع ملف `.bridgeignore` بانتظام

## 📞 المساعدة

```bash
python3 bridge_tool/cli.py --help
python3 bridge_tool/cli.py <command> --help
```

---

**الإصدار:** 1.0.0  
**الترخيص:** MIT
