تمام، الآن فهمت قصدك 🔥
أنت تقصد اختبار الوجهات (Navigation Testing) أي اختبار انتقال الصفحات والروابط والمسارات داخل المواقع باستخدام Selenium.

إليك كل ما تحتاجه:


---

✅ المكتبات المستخدمة لاختبار "الوجهات" والمسارات في Selenium

1. Selenium WebDriver

هذه الأساس، وتقوم بـ:

فتح الصفحات

الضغط على الروابط

اختبار المسار الصحيح

التأكد من عدم وجود 404, 500

فحص العنوان Title بعد الانتقال



---

2. SeleniumBase (ممتاز جدًا)

مبني فوق Selenium ومخصص للاختبار الآلي.

يقدم لك:

فحص الروابط تلقائيًا

التأكد من أن كل صفحة تعمل

التقاط سكرين عند الفشل

Assertions جاهزة



---

3. Playwright (بديل قوي وأسرع من Selenium)

يدعم:

فحص الروابط

التنقل بين الصفحات

التأكد من الاستجابة

الانتظار التلقائي (auto-wait)



---

4. pytest + selenium

لتنظيم الاختبارات مثل:

test_navigation_home_to_login

test_links_work

test_buttons_open_correct_page



---

5. BeautifulSoup (فقط لفحص الروابط بدون تشغيل متصفح)

إذا أردت فقط:

جمع الروابط

فحص الاستجابة (200–404–500)


بدون محاكاة حقيقية للتصفح.


---

🎯 أهم الاختبارات المتعلقة بالوجهات Navigation Tests

✔️ 1. اختبار رابط معين يعمل

تفحص أنه لا يعطي 404

ينتقل للصفحة الصحيحة


✔️ 2. اختبار أزرار التنقل الرئيسة (Navbar)

Home → Login
Login → Dashboard
Dashboard → Settings
… إلخ

✔️ 3. اختبار عمليات التحويل Redirects

مثل:

غير مسجل → Login redirect

بعد تسجيل الدخول → Dashboard redirect


✔️ 4. اختبار الروابط الداخلية والخارجية

Internal links must open correct pages
External links must open new tab

✔️ 5. اختبار صفحات الدفع / التسجيل / التسجيل الناقص

للتحقق من:

المسارات

الروابط

الـ flows



---

🔥 مثال كامل بالـ Selenium لاختبار الوجهات

📌 Python + Selenium

from selenium import webdriver
from selenium.webdriver.common.by import By
from selenium.webdriver.chrome.service import Service

driver = webdriver.Chrome()

# افتح الصفحة الرئيسية
driver.get("https://example.com")

# اضغط زر Login
login_button = driver.find_element(By.XPATH, "//a[@href='/login']")
login_button.click()

# تأكد أن الوجهة صحيحة
assert "login" in driver.current_url

# اضغط Register
register_link = driver.find_element(By.XPATH, "//a[@href='/register']")
register_link.click()

# تأكد من الوجهة
assert "register" in driver.current_url

driver.quit()


---

🔥 مثال لفحص جميع الروابط في الصفحة

import requests
from selenium import webdriver
from selenium.webdriver.common.by import By

driver = webdriver.Chrome()
driver.get("https://example.com")

links = driver.find_elements(By.TAG_NAME, "a")

for link in links:
    url = link.get_attribute("href")
    if url:
        r = requests.get(url)
        print(url, r.status_code)


---

🎯 إذا تريد، أقدر أعطيك:

✔️ نظام اختبار كامل للوجهات في موقعك
✔️ هيكل مشروع Selenium احترافي
✔️ كود يعمل على Termux
✔️ اختبارات دفع + تسجيل + دخول + روابط + صفحات غير مكتملة

فقط قل لي:
هل تريد أن أبني لك ملف مشروع جاهز لاختبار الوجهات؟