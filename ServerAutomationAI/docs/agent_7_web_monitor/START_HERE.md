# 🚀 ابدأ من هنا - دليل سريع للوكيل المطور

> **مهم:** اقرأ هذا الملف أولاً قبل أي شيء!

---

## 📖 ترتيب القراءة الإلزامي

اقرأ الملفات بهذا الترتيب بالضبط:

### 1️⃣ هذا الملف (أنت هنا الآن)
**الهدف:** فهم الخطوات الأولى

### 2️⃣ CORRECTIONS.md
**الهدف:** تجنب الأخطاء الحرجة  
**⚠️ حرج جداً:** يحتوي على تصحيحات لأخطاء في الخطة الأصلية

### 3️⃣ README.md
**الهدف:** نظرة عامة على المشروع

### 4️⃣ WEB_MONITOR_AGENT_7_IMPLEMENTATION_PLAN.md
**الهدف:** الخطة الرئيسية + المراحل 0-1  
**ملاحظة:** استخدم التصحيحات من CORRECTIONS.md

### 5️⃣ WEB_MONITOR_AGENT_7_TASKS_DETAILED.md
**الهدف:** المراحل 2-6 + أمثلة الكود

---

## ✅ Checklist قبل البدء

- [ ] قرأت CORRECTIONS.md كاملاً
- [ ] فهمت أن `tools/agent_communication.py` هو الصحيح (وليس `tools/communication.py`)
- [ ] فهمت هيكل الملفات (main في `agents/web_monitor_agent.py`)
- [ ] عرفت أن الوكيل يعمل على السيرفر الخارجي (وليس Replit)
- [ ] اختبرت الاتصال بالسيرفر عبر Bridge Tool

---

## 🔧 الخطوات الأولى (يجب تنفيذها)

### الخطوة 1: اختبار الاتصال

```bash
# من Replit
python3 bridge_tool/cli.py test
```

**النتيجة المتوقعة:**
```
✓ All tests passed!
```

---

### الخطوة 2: فحص البنية الموجودة

```bash
# فحص الملفات الموجودة
python3 bridge_tool/cli.py exec "ls -la /srv/ai_system/agents/"
python3 bridge_tool/cli.py exec "ls -la /srv/ai_system/tools/"

# قراءة الملف الصحيح للتواصل
python3 bridge_tool/cli.py exec "head -50 /srv/ai_system/tools/agent_communication.py"
```

---

### الخطوة 3: فحص التطبيق المراقَب

```bash
# فحص التطبيق
python3 bridge_tool/cli.py exec "ls -la /home/administrator/Bot.v4"
python3 bridge_tool/cli.py exec "cat /home/administrator/Bot.v4/package.json | head -50"

# التحقق من أن التطبيق يعمل
python3 bridge_tool/cli.py exec "ps aux | grep node"
python3 bridge_tool/cli.py exec "curl -I http://localhost:3000"
```

---

### الخطوة 4: أخذ نسخة احتياطية

```bash
# نسخة احتياطية من الوكلاء الموجودين
python3 bridge_tool/cli.py exec "python /srv/ai_system/agents/backup_recovery.py"

# نسخة احتياطية من التطبيق المراقَب
python3 bridge_tool/cli.py exec "cd /home/administrator && tar -czf Bot.v4_backup_$(date +%Y%m%d_%H%M%S).tar.gz Bot.v4"
```

---

## 🎯 المهمة الأولى

**ابدأ بالمرحلة 0، المهمة 0.1** من ملف WEB_MONITOR_AGENT_7_IMPLEMENTATION_PLAN.md

**الميزانية:** 200,000 توكن  
**المدة:** يوم واحد

**المطلوب:**
- فحص شامل للتطبيق
- إنشاء ملف `docs/agent_7_web_monitor/bot_v4_analysis.md`
- جدول بجميع endpoints
- قائمة بجميع الصفحات

---

## ⚠️ تذكيرات مهمة

1. **استخدم المسارات الصحيحة:**
   - ✅ `tools/agent_communication.py`
   - ❌ `tools/communication.py`

2. **الملف الرئيسي:**
   - ✅ `agents/web_monitor_agent.py`
   - الوحدات الفرعية في `agents/web_monitor/`

3. **لا تكرار:**
   - استخدم `tools/logger.py` الموجود
   - استخدم `tools/agent_communication.py` الموجود
   - لا تنشئ ملفات جديدة لنفس الوظائف

4. **معالجة الفشل:**
   - كل function يجب أن يحتوي على try/except
   - استخدم fallback methods (انظر CORRECTIONS.md)

5. **النسخ الاحتياطي:**
   - قبل أي تعديل، خذ نسخة احتياطية
   - سجل كل خطوة في PROGRESS.md

---

## 📞 الاتصال والنشر

```bash
# الاختبار المحلي (Replit)
python agents/web_monitor_agent.py

# النشر للسيرفر
python3 bridge_tool/cli.py push

# التحقق من السجلات
python3 bridge_tool/cli.py exec "tail -f /srv/ai_system/logs/web_monitor.log"
```

---

## 🆘 إذا واجهت مشكلة

1. **راجع CORRECTIONS.md**
2. **تحقق من السجلات:**
   ```bash
   python3 bridge_tool/cli.py exec "tail -100 /srv/ai_system/logs/web_monitor.log"
   ```
3. **اتصل بالمراجع:**
   - استخدم أداة `architect` للمراجعة
   - قدم الملفات المعنية والخطأ

---

**الآن أنت جاهز! ابدأ بقراءة CORRECTIONS.md ثم الخطة الرئيسية. 🚀**
