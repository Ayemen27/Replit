# 📋 قائمة التحقق للنشر (Deployment Checklist)

استخدم هذه القائمة قبل وبعد كل عملية نشر لضمان نجاح العملية.

---

## ✅ قبل النشر (Pre-Deployment)

### 1. التحضير

- [ ] تم إضافة all التغييرات في Git
- [ ] لا توجد أخطاء واضحة في الكود
- [ ] تم اختبار التغييرات محلياً (إذا أمكن)
- [ ] تم مراجعة ملفات الإعداد (`configs/config.yaml`)
- [ ] تم تحديث `requirements.txt` إذا أضفت مكتبات جديدة

### 2. الاتصال والبنية التحتية

- [ ] اختبار اتصال SSH: `python3 bridge_tool/cli.py test`
- [ ] التأكد من توفر مساحة كافية على السيرفر
- [ ] التأكد من عمل قاعدة البيانات على السيرفر
- [ ] التحقق من حالة النظام الحالية: `python3 bridge_tool/cli.py status`

### 3. النسخ الاحتياطي

- [ ] التأكد من تفعيل النسخ الاحتياطي التلقائي
- [ ] أو عمل نسخة احتياطية يدوية قبل النشر:
  ```bash
  python3 bridge_tool/cli.py exec "python3 src/setup/backup_database.py"
  ```

### 4. الاختبار الوهمي (Dry Run)

- [ ] تشغيل نشر وهمي: `python3 bridge_tool/cli.py push --dry-run`
- [ ] مراجعة الملفات التي سيتم نقلها
- [ ] التأكد من عدم وجود ملفات حساسة سيتم نقلها

---

## 🚀 أثناء النشر (During Deployment)

### 1. تنفيذ النشر

- [ ] بدء النشر: `python3 bridge_tool/cli.py push`
- [ ] مراقبة عملية النقل للتأكد من عدم وجود أخطاء
- [ ] الانتظار حتى اكتمال جميع الخطوات

### 2. مراقبة الإخراج

- [ ] التأكد من نجاح نقل الملفات
- [ ] التأكد من نجاح تنفيذ السكريبتات
- [ ] التأكد من تحديث symlink `current`
- [ ] التأكد من إعادة تشغيل الخدمة

---

## ✅ بعد النشر (Post-Deployment)

### 1. التحقق الأساسي (خلال 5 دقائق)

- [ ] فحص حالة النظام: `python3 bridge_tool/cli.py status`
- [ ] التأكد من عمل الخدمة:
  ```bash
  python3 bridge_tool/cli.py exec "systemctl is-active ai_agents"
  ```
- [ ] فحص حالة الوكلاء الستة:
  ```bash
  python3 bridge_tool/cli.py exec "python3 main.py status"
  ```

### 2. فحص السجلات (خلال 10 دقائق)

- [ ] جلب أحدث السجلات: `python3 bridge_tool/cli.py pull logs`
- [ ] البحث عن أخطاء:
  ```bash
  grep -i "error\|critical\|exception" pulled_logs/*.log
  ```
- [ ] التأكد من عدم وجود تحذيرات غير متوقعة

### 3. اختبار الوظائف الأساسية

- [ ] اختبار Performance Monitor:
  ```bash
  python3 bridge_tool/cli.py exec "timeout 10 python3 agents/performance_monitor.py"
  ```
- [ ] اختبار Database Manager:
  ```bash
  python3 bridge_tool/cli.py exec "python3 agents/database_manager.py"
  ```
- [ ] اختبار Log Analyzer:
  ```bash
  python3 bridge_tool/cli.py exec "timeout 10 python3 agents/log_analyzer.py"
  ```
- [ ] اختبار Security Monitor:
  ```bash
  python3 bridge_tool/cli.py exec "timeout 10 python3 agents/security_monitor.py"
  ```
- [ ] اختبار Backup Agent:
  ```bash
  python3 bridge_tool/cli.py exec "python3 agents/backup_recovery.py"
  ```
- [ ] اختبار AI Manager:
  ```bash
  python3 bridge_tool/cli.py exec "timeout 10 python3 agents/ai_manager.py"
  ```

### 4. اختبار قاعدة البيانات

- [ ] التأكد من اتصال قاعدة البيانات
- [ ] فحص حجم قاعدة البيانات
- [ ] التأكد من عمل النسخ الاحتياطي التلقائي

### 5. اختبار التنبيهات (إذا مفعلة)

- [ ] اختبار Telegram notifications (إذا مفعلة)
- [ ] اختبار Email notifications (إذا مفعلة)

### 6. المراقبة المستمرة (خلال ساعة)

- [ ] مراقبة استخدام CPU/RAM
- [ ] مراقبة مساحة القرص
- [ ] مراقبة السجلات للأخطاء الجديدة
- [ ] التأكد من عمل جميع الوكلاء بشكل صحيح

---

## 🆘 في حالة وجود مشاكل

### مشكلة طفيفة (Minor Issue)

- [ ] توثيق المشكلة
- [ ] محاولة الإصلاح اليدوي
- [ ] اختبار الإصلاح
- [ ] التحديث في PROGRESS.md

### مشكلة حرجة (Critical Issue)

- [ ] **التراجع فوراً:**
  ```bash
  python3 bridge_tool/cli.py rollback
  ```
- [ ] التحقق من استعادة الخدمة
- [ ] توثيق المشكلة بالتفصيل
- [ ] تحليل السبب قبل النشر مرة أخرى

---

## 📊 مقاييس النجاح

اعتبر النشر ناجحاً إذا:

- ✅ جميع الوكلاء الستة تعمل بدون أخطاء
- ✅ لا توجد أخطاء CRITICAL في السجلات
- ✅ الخدمة تعمل (active) في systemd
- ✅ قاعدة البيانات متصلة وتعمل
- ✅ النسخ الاحتياطي يعمل بشكل طبيعي
- ✅ استخدام الموارد ضمن الحدود الطبيعية

---

## 📝 التوثيق

بعد النشر الناجح:

- [ ] تحديث PROGRESS.md بتاريخ النشر
- [ ] توثيق أي تغييرات مهمة
- [ ] توثيق أي مشاكل تم حلها
- [ ] تحديث رقم الإصدار إذا لزم الأمر

---

## 🔄 الصيانة الدورية

### يومياً

- [ ] فحص الحالة: `python3 bridge_tool/cli.py status`
- [ ] مراجعة السجلات بحثاً عن أخطاء

### أسبوعياً

- [ ] جلب جميع السجلات والنسخ الاحتياطية
- [ ] فحص مساحة القرص
- [ ] مراجعة أداء الوكلاء

### شهرياً

- [ ] مراجعة شاملة لجميع المكونات
- [ ] تحديث المكتبات والتبعيات
- [ ] مراجعة وتنظيف السجلات القديمة
- [ ] اختبار عملية الاسترجاع من النسخ الاحتياطية

---

## ✨ نصائح إضافية

1. **لا تستعجل:** خذ وقتك في التحقق من كل خطوة
2. **احتفظ بسجل:** وثق كل عملية نشر
3. **كن مستعداً للتراجع:** يمكن التراجع بأمان في أي وقت
4. **راقب الموارد:** تأكد من عدم استنزاف موارد السيرفر
5. **اختبر في بيئة مشابهة أولاً:** إذا أمكن

---

**آخر تحديث:** 2025-11-14  
**الإصدار:** 1.0.0
