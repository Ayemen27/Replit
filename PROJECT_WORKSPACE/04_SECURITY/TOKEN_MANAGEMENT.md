# 🤖 نظام إدارة الذكاء الاصطناعي والمفاتيح

> **📍 الوثيقة المرجعية**: دليل شامل لنظام إدارة مفاتيح API للذكاء الاصطناعي في المنصة

**آخر تحديث**: 2025-11-18  
**الحالة**: ✅ موثق بالكامل (Phase 1 مُنفّذ، Phase 2-5 موثقة ومخططة)  
**المسؤول**: Platform Architecture Team

---

## 📋 المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [النظام الحالي (Phase 1)](#النظام-الحالي-phase-1)
3. [التحسينات المخططة](#التحسينات-المخططة)
4. [Architecture المقترحة](#architecture-المقترحة)
5. [سيناريوهات الاستخدام](#سيناريوهات-الاستخدام)
6. [خطة التنفيذ](#خطة-التنفيذ)
7. [أمثلة عملية](#أمثلة-عملية)

---

## 🎯 نظرة عامة

### المشكلة الأساسية

كيف نوفر ذكاء اصطناعي قوي (GPT-4 مستوى) للمنصة **بدون** استهلاك موارد ضخمة؟

### الحل المعماري

```
❌ الحل التقليدي (غير مناسب):
   تشغيل نماذج AI محلياً → يحتاج GPU + RAM كبيرة + تكلفة عالية

✅ حلنا الذكي:
   استخدام API خارجية (Groq, Gemini, Mistral) → خفيف + قوي + تكلفة منخفضة
```

### المبدأ الأساسي

```
المنصة (Control Plane)
    ↓ API Call (عبر الإنترنت)
AI Provider (Groq/Gemini/Mistral)
    ↓ Response
المنصة → تنفيذ على سيرفر المستخدم
```

**النتيجة**:
- ✅ المنصة خفيفة جداً ($5-10/mo)
- ✅ ذكاء اصطناعي قوي (GPT-4 مستوى)
- ✅ لا يستهلك موارد سيرفر المستخدم
- ✅ تكلفة على المستخدم: $0-10/mo (حسب الاستخدام)

---

## 📊 النظام الحالي (Phase 1)

### المكونات الموجودة

#### 1. **SecretsManager** (`secrets_manager.py`)

**الدور**: تخزين وإدارة المفاتيح بشكل آمن

```python
# الميزات الحالية:
✅ Fernet encryption للمفاتيح الحساسة
✅ .env file support
✅ Encrypted storage (data/secrets.enc)
✅ صلاحيات ملفات آمنة (chmod 0o600)
✅ دعم 4 providers: Groq, Gemini, Mistral, HuggingFace
```

**مثال الاستخدام**:
```python
from dev_platform.core.secrets_manager import get_secrets_manager

secrets = get_secrets_manager()

# إضافة مفتاح
secrets.set("GROQ_API_KEY", "sk-xxx...")

# قراءة مفتاح
api_key = secrets.get("GROQ_API_KEY")

# التحقق من المفاتيح المتاحة
keys_status = secrets.validate_model_keys()
# → {"groq": True, "gemini": False, "mistral": True, ...}
```

**القيود الحالية**:
- ❌ مفتاح واحد فقط لكل provider
- ❌ لا يتتبع الاستهلاك اليومي
- ❌ لا يدعم multiple keys للـ provider نفسه

---

#### 2. **ModelRouter** (`model_router.py`)

**الدور**: توجيه الطلبات للنماذج المتاحة مع failover ذكي

```python
# الميزات الحالية:
✅ Automatic failover بين providers مختلفة
✅ Health scoring (0-100) لكل provider
✅ Retry mechanism مع exponential backoff + jitter
✅ Response caching (1 ساعة)
✅ Test ping لاختبار المفاتيح
✅ Quarantine system للمفاتيح الفاشلة (5 دقائق)
✅ Recovery probes لاستعادة providers
✅ Graceful degradation (heuristic guidance)
```

**الـ Providers المدعومة** (بالترتيب حسب السرعة):

| Provider | Model | Max Tokens | السرعة | الحد المجاني اليومي |
|----------|-------|------------|--------|---------------------|
| **Groq** | llama-3.3-70b-versatile | 8000 | ⚡ Fastest | 14,400 tokens/day |
| **Gemini** | gemini-1.5-flash | 8000 | ⚡⚡ Very Fast | 1,500 requests/day |
| **Mistral** | mistral-large-latest | 8000 | ⚡⚡⚡ Fast | متغير |

**مثال الاستخدام**:
```python
from dev_platform.core.model_router import ModelRouter

router = ModelRouter()

# طلب عادي
response = router.chat(
    messages=[
        {"role": "user", "content": "كيف أنشئ API في FastAPI؟"}
    ],
    temperature=0.7,
    max_tokens=500
)

# النتيجة
print(response["content"])  # الرد من الذكاء الاصطناعي
print(response["model"])    # "groq/llama-3.3-70b-versatile"
print(response["tokens_used"])  # 234
print(response["time_taken"])   # 1.23 ثانية
```

**الـ Flow الكامل**:
```
1. المستخدم يطلب
   ↓
2. router.chat() يستقبل الطلب
   ↓
3. يتحقق من Cache (هل طُلب من قبل؟)
   ├─ نعم → يرجع من Cache (سريع جداً)
   └─ لا → يواصل
   ↓
4. يرتب Providers حسب Health Score
   Groq (95%) → Gemini (100%) → Mistral (80%)
   ↓
5. يحاول Groq أولاً
   ├─ نجح ✅ → يرجع النتيجة + يحفظ في Cache
   ├─ Rate Limit ⚠️ → ينتقل لـ Gemini
   ├─ Auth Error ❌ → Quarantine لـ 5 دقائق → ينتقل
   └─ Timeout ⏱️ → Retry مع backoff → ينتقل
   ↓
6. إذا فشلت جميع Providers
   → Graceful Degradation (heuristic guidance)
```

**Health Scoring System**:
```python
# كل provider له health score (0-100)

عند النجاح:
    score = min(100, current_score + 5)  # يزيد تدريجياً

عند الفشل:
    score = max(0, current_score - 15)   # ينخفض بسرعة

عند score < 20:
    - يُتخطى عادةً
    - 20% فرصة لـ recovery probe

عند score = 0:
    - Quarantine لـ 5 دقائق
    - لا يُستخدم إطلاقاً
```

**القيود الحالية**:
- ❌ لا يدعم multiple keys لنفس ال provider
- ❌ لا يتتبع الحد اليومي المجاني بدقة
- ❌ يعتمد فقط على error من API (reactive، ليس proactive)
- ❌ لا نظام إشعارات عند المشاكل

---

### التدفق الحالي الكامل

```
┌─────────────────────────────────────────────────────────────┐
│  المستخدم يطلب: "حلل هذا الخطأ في الكود"                   │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  Platform Agent (على VPS المستخدم)                         │
│  → يستقبل الطلب                                            │
│  → يجهز السياق (logs, code, error)                         │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  ModelRouter.chat()                                         │
│  → يبحث في Cache (لا يوجد)                                │
│  → يختار Provider (Groq - health: 95%)                     │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ↓ HTTPS API Call
┌─────────────────────────────────────────────────────────────┐
│  Groq API (خارجي - على السحابة)                           │
│  → يستقبل الطلب                                            │
│  → يعالج بـ Llama 3.3 70B                                  │
│  → يرجع: "الخطأ بسبب..."                                   │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  ModelRouter                                                │
│  → يستقبل الرد                                             │
│  → يسجل الاستخدام: 234 tokens, 1.2s                       │
│  → يحدث Health Score: 95 → 100                             │
│  → يحفظ في Cache                                           │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  Platform Agent                                             │
│  → يستقبل التحليل                                          │
│  → ينفذ الحل على السيرفر                                  │
│  → يرسل نتيجة للمستخدم                                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚀 التحسينات المخططة

### Phase 2: Multi-Key Rotation ⭐⭐⭐⭐⭐

**المشكلة**:
```
حالياً:
GROQ_API_KEY = "key1"  ← مفتاح واحد فقط

عند انتهاء حده (14,400 tokens/day):
→ ينتقل لـ Gemini (provider مختلف تماماً!)
→ لا يمكن استخدام Groq بقية اليوم ❌
```

**الحل**:
```python
# .env file:
GROQ_API_KEY_1=sk-proj-xxx  # Primary (Priority 1)
GROQ_KEY_1_PRIORITY=1
GROQ_KEY_1_DAILY_LIMIT=14400

GROQ_API_KEY_2=sk-proj-yyy  # Backup (Priority 2)
GROQ_KEY_2_PRIORITY=2
GROQ_KEY_2_DAILY_LIMIT=14400

GROQ_API_KEY_3=sk-proj-zzz  # Emergency (Priority 3)
GROQ_KEY_3_PRIORITY=3
GROQ_KEY_3_DAILY_LIMIT=14400
```

**التدفق الجديد**:
```
Groq Key 1 (14,400 tokens متبقي) → يُستخدم
   ↓ بعد 10,000 طلب
Groq Key 1 (4,400 tokens متبقي) → لا يزال يُستخدم
   ↓ بعد 4,500 طلب
Groq Key 1 (0 tokens - مستنفد!) → يُوقف تلقائياً ✅
   ↓ تبديل فوري
Groq Key 2 (14,400 tokens متبقي) → يُستخدم الآن ✅
   ↓ نفس السرعة، نفس الجودة!
```

**الفوائد**:
- ✅ لا انقطاع في الخدمة
- ✅ استمرار استخدام نفس ال provider (أسرع)
- ✅ توزيع الحمل على مفاتيح متعددة
- ✅ مرونة أكبر

---

### Phase 3: Daily Quota Tracking ⭐⭐⭐⭐⭐

**المشكلة**:
```
حالياً:
- لا نعرف كم استهلكنا من الحد اليومي
- نكتشف فقط عند error من API
- reactive بدلاً من proactive
```

**الحل**:
```python
class QuotaTracker:
    def log_usage(provider, key_id, tokens_used):
        """تسجيل كل استخدام"""
        today = "2025-11-18"
        cache_key = f"quota_{provider}_{key_id}_{today}"
        
        current = cache.get(cache_key) or 0
        new_total = current + tokens_used
        
        cache.set(cache_key, new_total, expire=86400)  # 24h
        
        return new_total
    
    def get_remaining_quota(provider, key_id, daily_limit):
        """كم تبقى من الحد اليومي؟"""
        used = self.get_today_usage(provider, key_id)
        remaining = max(0, daily_limit - used)
        
        return {
            "used": used,              # 10,234
            "limit": daily_limit,      # 14,400
            "remaining": remaining,    # 4,166
            "percentage": 71.0         # استُهلك 71%
        }
```

**الاستخدام الذكي**:
```python
# قبل كل طلب:
quota_info = quota_tracker.get_remaining_quota("groq", 1, 14400)

if quota_info["remaining"] < 100:
    # قريب من الانتهاء!
    logger.warning(f"Groq Key 1: {quota_info['percentage']}% used")
    # انتقل للمفتاح التالي تلقائياً
    use_next_key()

if quota_info["percentage"] > 90:
    # إشعار للمسؤول
    send_notification("⚠️ Groq Key 1 at 90% quota!")
```

**الفوائد**:
- ✅ معرفة دقيقة بالاستهلاك
- ✅ تحذيرات قبل انتهاء الحد
- ✅ استخدام أمثل للمفاتيح
- ✅ تخطيط أفضل للموارد

---

### Phase 4: Notification System ⭐⭐⭐⭐

**المشكلة**:
```
حالياً:
- عند فشل جميع المفاتيح → المستخدم يرى خطأ فقط
- لا إشعار للمسؤول
- لا تنبيهات استباقية
```

**الحل**:
```python
class KeyManagementNotifier:
    def alert_quota_warning(provider, key_id, percentage):
        """تحذير عند اقتراب انتهاء الحد"""
        if percentage > 90:
            send_telegram("🚨 {provider} Key {key_id}: {percentage}% used!")
            send_email(subject=f"Critical: {provider} quota")
        elif percentage > 75:
            send_telegram("⚠️ {provider} Key {key_id}: {percentage}% used")
    
    def alert_all_keys_failed(errors_by_provider):
        """إشعار حرج: جميع المفاتيح فشلت"""
        send_email(
            subject="🚨 CRITICAL: All AI Keys Failed!",
            body=f"Immediate action required:\n{errors_by_provider}"
        )
        send_telegram("🚨 All AI providers down!")
        dashboard_alert("critical", "All AI keys failed")
    
    def alert_key_authentication_failed(provider, key_id):
        """مفتاح معين فشل (منتهي/خاطئ)"""
        send_email(
            subject=f"❌ {provider} Key {key_id} Auth Failed",
            body="Please check and update the API key"
        )
```

**قنوات الإشعار**:
- ✅ **Telegram**: للتنبيهات الفورية
- ✅ **Email**: للأمور الحرجة
- ✅ **Dashboard Alerts**: في الواجهة
- ✅ **Logs**: دائماً

**الفوائد**:
- ✅ رد فعل سريع للمشاكل
- ✅ معرفة فورية بالأعطال
- ✅ تقليل وقت التوقف

---

### Phase 5: Monitoring Dashboard ⭐⭐⭐

**المشكلة**:
```
حالياً:
- لا رؤية واضحة للمفاتيح
- لا إحصائيات استخدام
- إدارة يدوية معقدة
```

**الحل**: Dashboard متكامل

```
/admin/api-keys
┌──────────────────────────────────────────────────────────┐
│ 🔑 API Keys Management Dashboard                        │
├──────────────────────────────────────────────────────────┤
│                                                          │
│ 📊 Overview (Last 24 hours)                             │
│ ┌────────────────────────────────────────────────────┐  │
│ │ Total Requests: 1,234                              │  │
│ │ Total Tokens: 45,678                               │  │
│ │ Success Rate: 99.03% (12 failed)                   │  │
│ │ Avg Response Time: 1.2s                            │  │
│ └────────────────────────────────────────────────────┘  │
│                                                          │
│ 🟢 Groq (3 keys) - Health: 95%                          │
│ ┌────────────────────────────────────────────────────┐  │
│ │ Key 1 (Primary)        ████████░░ 80% (11.5K/14.4K)│✅│
│ │ Key 2 (Backup)         ██████████ 100% (14.4K/14.4K│🔴│
│ │ Key 3 (Emergency)      ███░░░░░░░ 30% (4.3K/14.4K) │✅│
│ │                                                     │  │
│ │ Resets in: 4h 23m                                  │  │
│ └────────────────────────────────────────────────────┘  │
│                                                          │
│ 🟢 Gemini (2 keys) - Health: 100%                       │
│ ┌────────────────────────────────────────────────────┐  │
│ │ Key 1                  ████░░░░░░ 40% (600/1500)   │✅│
│ │ Key 2                  █░░░░░░░░░ 10% (150/1500)   │✅│
│ │                                                     │  │
│ │ Resets in: 4h 23m                                  │  │
│ └────────────────────────────────────────────────────┘  │
│                                                          │
│ 📈 Usage Graph (Last 7 days)                            │
│ [Interactive Chart Here]                                │
│                                                          │
│ [Add New Key] [Refresh] [Export Report] [Settings]     │
└──────────────────────────────────────────────────────────┘
```

**الميزات**:
- ✅ رؤية فورية لجميع المفاتيح
- ✅ إحصائيات استخدام تفصيلية
- ✅ تنبيهات بصرية (🔴🟡🟢)
- ✅ إدارة سهلة للمفاتيح
- ✅ تصدير تقارير

---

## 🏗️ Architecture المقترحة

### النظام الكامل (بعد جميع التحسينات)

```
┌─────────────────────────────────────────────────────────────┐
│                    Control Plane (المنصة)                   │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │              SecretsManager (Enhanced)                │ │
│  │  ┌─────────────────────────────────────────────────┐ │ │
│  │  │ Provider: Groq                                   │ │ │
│  │  │  ├─ Key 1: sk-xxx (Priority: 1, Quota: 80%)    │ │ │
│  │  │  ├─ Key 2: sk-yyy (Priority: 2, Quota: 100%)   │ │ │
│  │  │  └─ Key 3: sk-zzz (Priority: 3, Quota: 30%)    │ │ │
│  │  │                                                  │ │ │
│  │  │ Provider: Gemini                                 │ │ │
│  │  │  ├─ Key 1: AI-xxx (Priority: 1, Quota: 40%)    │ │ │
│  │  │  └─ Key 2: AI-yyy (Priority: 2, Quota: 10%)    │ │ │
│  │  └─────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │              ModelRouter (Enhanced)                   │ │
│  │  ┌─ Health Scoring                                   │ │
│  │  ├─ Multi-Key Selection (smart)                      │ │
│  │  ├─ Quota-Aware Routing                              │ │
│  │  ├─ Automatic Failover                               │ │
│  │  └─ Response Caching                                 │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │              QuotaTracker (New)                       │ │
│  │  ├─ Usage Logging (per key, per day)                 │ │
│  │  ├─ Remaining Quota Calculation                      │ │
│  │  ├─ Warning Triggers (75%, 90%)                      │ │
│  │  └─ Daily Reset (00:00 UTC)                          │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │           KeyManagementNotifier (New)                 │ │
│  │  ├─ Telegram Alerts                                   │ │
│  │  ├─ Email Notifications                               │ │
│  │  ├─ Dashboard Alerts                                  │ │
│  │  └─ Log Warnings                                      │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │           Keys Dashboard (New)                        │ │
│  │  ├─ Real-time Key Status                              │ │
│  │  ├─ Usage Charts                                      │ │
│  │  ├─ Health Monitoring                                 │ │
│  │  └─ Key Management UI                                 │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                           │
                           ↓ API Calls
            ┌──────────────┴──────────────┐
            │                             │
            ↓                             ↓
┌───────────────────┐         ┌───────────────────┐
│  Groq API         │         │  Gemini API       │
│  (External)       │         │  (External)       │
└───────────────────┘         └───────────────────┘
```

---

## 📝 سيناريوهات الاستخدام

### Scenario 1: استخدام عادي (Happy Path)

```
08:00 AM - بداية اليوم
├─ Groq Key 1: 0 tokens used (0%)
├─ Groq Key 2: 0 tokens used (0%)
└─ Groq Key 3: 0 tokens used (0%)

10:00 AM - بعد 500 طلب
├─ Groq Key 1: 5,000 tokens used (35%) ✅ يُستخدم
├─ Groq Key 2: 0 tokens used (0%)
└─ Groq Key 3: 0 tokens used (0%)

02:00 PM - بعد 1,200 طلب
├─ Groq Key 1: 12,000 tokens used (83%) ⚠️ تحذير 75%
├─ Groq Key 2: 0 tokens used (0%)
└─ Groq Key 3: 0 tokens used (0%)
    ↓ إشعار Telegram
    "⚠️ Groq Key 1: 83% quota used"

04:00 PM - بعد 1,450 طلب
├─ Groq Key 1: 14,400 tokens used (100%) 🔴 مستنفد!
├─ Groq Key 2: 0 tokens used (0%) ✅ يُستخدم الآن
└─ Groq Key 3: 0 tokens used (0%)
    ↓ تبديل تلقائي فوري
    ✅ لا انقطاع في الخدمة

06:00 PM - استمرار العمل
├─ Groq Key 1: 14,400 tokens used (100%) 🔴 معطل
├─ Groq Key 2: 3,200 tokens used (22%) ✅ يُستخدم
└─ Groq Key 3: 0 tokens used (0%)

12:00 AM - إعادة تعيين يومية
├─ Groq Key 1: 0 tokens used (0%) ✅ جاهز
├─ Groq Key 2: 0 tokens used (0%) ✅ جاهز
└─ Groq Key 3: 0 tokens used (0%) ✅ جاهز
```

### Scenario 2: فشل مفتاح (Authentication Error)

```
Request #1234
├─ ModelRouter يختار: Groq Key 1
├─ يرسل الطلب
└─ Error: AuthenticationError (مفتاح منتهي/خاطئ)
    ↓
    ┌─ Quarantine: Groq Key 1 لـ 5 دقائق
    ├─ Health Score: 100 → 0
    ├─ Cache validation failure: 5 دقائق
    └─ إشعار فوري
        ├─ Email: "❌ Groq Key 1 authentication failed"
        └─ Telegram: "❌ Groq Key 1 invalid - check API key"
    ↓
    Retry with: Groq Key 2
    └─ ✅ نجح!
```

### Scenario 3: فشل جميع المفاتيح (Critical)

```
Request #5678
├─ Groq Key 1: AuthenticationError
├─ Groq Key 2: AuthenticationError
├─ Groq Key 3: AuthenticationError
├─ Gemini Key 1: RateLimitError
└─ Gemini Key 2: RateLimitError
    ↓ جميع المفاتيح فشلت!
    ┌─ إشعارات حرجة:
    ├─ Email: "🚨 CRITICAL: All AI Keys Failed!"
    ├─ Telegram: "🚨 All AI providers down!"
    └─ Dashboard Alert: Red banner
    ↓
    Graceful Degradation:
    ├─ استخدام cached responses (إن وُجدت)
    └─ أو heuristic guidance
        "I apologize, all AI models are unavailable.
         Please try again in a few moments..."
```

---

## 📅 خطة التنفيذ

### Phase 2: Multi-Key Support (أولوية 🔴🔴🔴🔴🔴)

**المدة**: 2-3 أيام  
**المطور**: Developer AI-Keys-01

**المهام**:
1. تعديل `SecretsManager.get_provider_keys()`
2. إضافة دعم `_API_KEY_1`, `_API_KEY_2`, إلخ في `.env`
3. تعديل `ModelRouter._check_available_models()`
4. إضافة key selection logic
5. Testing شامل

**المخرج**: ملف `DEVELOPER_AI_KEYS_01.md`

---

### Phase 3: Quota Tracking (أولوية 🔴🔴🔴🔴🔴)

**المدة**: 2-3 أيام  
**المطور**: Developer AI-Keys-02

**المهام**:
1. إنشاء `QuotaTracker` class
2. دمج مع `ModelRouter`
3. إضافة daily reset mechanism
4. Testing مع حدود حقيقية

**المخرج**: ملف `DEVELOPER_AI_KEYS_02.md`

---

### Phase 4: Notification System (أولوية 🟡🟡🟡)

**المدة**: 1-2 يوم  
**المطور**: Developer AI-Keys-03

**المهام**:
1. إنشاء `KeyManagementNotifier` class
2. ربط مع `notification_system.py` الموجود
3. إضافة trigger points في `ModelRouter`
4. Testing الإشعارات

**المخرج**: ملف `DEVELOPER_AI_KEYS_03.md`

---

### Phase 5: Monitoring Dashboard (أولوية 🟢🟢🟢)

**المدة**: 3-4 أيام  
**المطور**: Developer AI-Keys-04

**المهام**:
1. API endpoint: `/api/admin/keys-dashboard`
2. Frontend component (React/Next.js)
3. Real-time updates (WebSocket أو polling)
4. Charts & visualizations

**المخرج**: ملف `DEVELOPER_AI_KEYS_04.md`

---

## 💻 أمثلة عملية

### مثال 1: إعداد Multiple Keys

```bash
# .env file

# Groq Keys (3 مفاتيح)
GROQ_API_KEY_1=sk-proj-AAA...
GROQ_KEY_1_PRIORITY=1
GROQ_KEY_1_DAILY_LIMIT=14400

GROQ_API_KEY_2=sk-proj-BBB...
GROQ_KEY_2_PRIORITY=2
GROQ_KEY_2_DAILY_LIMIT=14400

GROQ_API_KEY_3=sk-proj-CCC...
GROQ_KEY_3_PRIORITY=3
GROQ_KEY_3_DAILY_LIMIT=14400

# Gemini Keys (2 مفاتيح)
GEMINI_API_KEY_1=AIza...XXX
GEMINI_KEY_1_PRIORITY=1
GEMINI_KEY_1_DAILY_LIMIT=1500

GEMINI_API_KEY_2=AIza...YYY
GEMINI_KEY_2_PRIORITY=2
GEMINI_KEY_2_DAILY_LIMIT=1500

# Notification Settings
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
TELEGRAM_CHAT_ID=987654321
ALERT_EMAIL=admin@platform.com
```

### مثال 2: الاستخدام في الكود

```python
from dev_platform.core.model_router import ModelRouter
from dev_platform.agents.base_agent import BaseAgent

class CodeAnalyzerAgent(BaseAgent):
    def __init__(self):
        super().__init__()
        self.model_router = ModelRouter()
    
    def analyze_error(self, error_log, code_snippet):
        """تحليل خطأ في الكود"""
        
        # تجهيز السياق
        context = f"""
        Error Log:
        {error_log}
        
        Code:
        {code_snippet}
        
        Please analyze this error and suggest a fix.
        """
        
        # طلب من الذكاء الاصطناعي
        response = self.model_router.chat(
            messages=[
                {"role": "system", "content": "You are an expert code analyzer."},
                {"role": "user", "content": context}
            ],
            temperature=0.3,  # دقة أكبر
            max_tokens=1000
        )
        
        # معالجة الرد
        analysis = response["content"]
        provider_used = response["provider"]  # "groq"
        tokens_used = response["tokens_used"]  # 456
        
        # تسجيل
        self.logger.info(f"Analysis completed via {provider_used} ({tokens_used} tokens)")
        
        # إذا كان هناك تحذير quota
        if response.get("quota_warning"):
            self.logger.warning(f"Quota warning: {response['quota_warning']}")
        
        return analysis
```

### مثال 3: مراقبة الاستخدام

```python
from dev_platform.core.model_router import ModelRouter

router = ModelRouter()

# الحصول على معلومات الاستخدام
usage_stats = router.quota_tracker.get_all_stats()

print(usage_stats)
# {
#   "groq": {
#     "key_1": {"used": 12000, "limit": 14400, "remaining": 2400, "percentage": 83.3},
#     "key_2": {"used": 0, "limit": 14400, "remaining": 14400, "percentage": 0.0},
#     "key_3": {"used": 0, "limit": 14400, "remaining": 14400, "percentage": 0.0}
#   },
#   "gemini": {
#     "key_1": {"used": 600, "limit": 1500, "remaining": 900, "percentage": 40.0},
#     "key_2": {"used": 150, "limit": 1500, "remaining": 1350, "percentage": 10.0}
#   }
# }

# التحقق من صحة المفاتيح
health_status = router.get_health_status()

print(health_status)
# {
#   "groq": {"health_score": 95, "keys": 3, "active": 2, "quarantined": 1},
#   "gemini": {"health_score": 100, "keys": 2, "active": 2, "quarantined": 0}
# }
```

---

## 🔗 الملفات ذات الصلة

**الوثائق**:
- [`ARCHITECTURE.md`](ARCHITECTURE.md) - البنية المعمارية الكاملة
- [`../../AGENT_TASKS/DEVELOPER_AI_KEYS_01.md`](../../AGENT_TASKS/DEVELOPER_AI_KEYS_01.md) - Multi-Key
- [`../../AGENT_TASKS/DEVELOPER_AI_KEYS_02.md`](../../AGENT_TASKS/DEVELOPER_AI_KEYS_02.md) - Quota Tracking
- [`../../AGENT_TASKS/DEVELOPER_AI_KEYS_03.md`](../../AGENT_TASKS/DEVELOPER_AI_KEYS_03.md) - Notifications
- [`../../AGENT_TASKS/DEVELOPER_AI_KEYS_04.md`](../../AGENT_TASKS/DEVELOPER_AI_KEYS_04.md) - Dashboard

**السيناريوهات**:
- [`../../05_OPERATIONS/RUNBOOKS/AI_KEY_ROTATION_SCENARIO.md`](../../05_OPERATIONS/RUNBOOKS/AI_KEY_ROTATION_SCENARIO.md)
- [`../../05_OPERATIONS/RUNBOOKS/QUOTA_EXCEEDED_SCENARIO.md`](../../05_OPERATIONS/RUNBOOKS/QUOTA_EXCEEDED_SCENARIO.md)
- [`../../05_OPERATIONS/RUNBOOKS/ALL_KEYS_FAILED_SCENARIO.md`](../../05_OPERATIONS/RUNBOOKS/ALL_KEYS_FAILED_SCENARIO.md)

**الكود**:
- `ServerAutomationAI/dev_platform/core/model_router.py`
- `ServerAutomationAI/dev_platform/core/secrets_manager.py`
- `ServerAutomationAI/dev_platform/core/quota_tracker.py` (جديد - سيُنشأ)
- `ServerAutomationAI/dev_platform/core/key_notifier.py` (جديد - سيُنشأ)

---

## 📊 ملخص المقارنة

| الميزة | Phase 1 (الحالي) | Phase 2-5 (المخطط) |
|--------|------------------|---------------------|
| **Multiple Keys per Provider** | ❌ مفتاح واحد فقط | ✅ غير محدود |
| **Quota Tracking** | ❌ reactive فقط | ✅ proactive دقيق |
| **Notifications** | ❌ لا يوجد | ✅ Email + Telegram + Dashboard |
| **Dashboard** | ❌ لا يوجد | ✅ مراقبة شاملة |
| **Intelligent Selection** | 🟡 health score فقط | ✅ multi-factor |
| **Auto-Rotation** | 🟡 بين providers | ✅ داخل نفس ال provider |
| **Usage Analytics** | 🟡 logs فقط | ✅ charts + stats |

---

**آخر تحديث**: 2025-11-18  
**المراجعة القادمة**: بعد كل Phase  
**المسؤول عن الصيانة**: Platform Team
