# ✅ معايير القبول - المرحلة 1: التثبيت والإعداد

## نظرة عامة

هذا المستند يحدد معايير القبول الواجب تحقيقها لاعتبار المرحلة 1 مكتملة بنجاح.

---

## 📦 1. تثبيت المكتبات

### المعايير:

#### ✅ 1.1 تثبيت ناجح
- [ ] تم تثبيت `@tolgee/react` بنجاح
- [ ] تم تثبيت `@tolgee/web` بنجاح
- [ ] تم تثبيت `@tolgee/i18n` بنجاح
- [ ] جميع المكتبات في `package.json`

#### ✅ 1.2 لا تعارضات
- [ ] `npm install` ينتهي بدون أخطاء
- [ ] لا توجد peer dependency warnings حرجة
- [ ] `npm run build` ينتهي بنجاح

#### 🧪 اختبار:
```bash
# يجب أن ينجح بدون أخطاء
npm install
npm run build
```

**النتيجة المتوقعة**: Build ناجح بدون أخطاء

---

## 🔐 2. متغيرات البيئة

### المعايير:

#### ✅ 2.1 ملف `.env.local` موجود
- [ ] الملف `.env.local` موجود في الجذر
- [ ] يحتوي على جميع المتغيرات المطلوبة:
  - `NEXT_PUBLIC_TOLGEE_API_URL`
  - `NEXT_PUBLIC_TOLGEE_API_KEY`
  - `TOLGEE_API_KEY`
  - `NEXT_PUBLIC_DEFAULT_LOCALE`
  - `NEXT_PUBLIC_SUPPORTED_LOCALES`
  - `NEXT_PUBLIC_FALLBACK_LOCALE`

#### ✅ 2.2 القيم صحيحة
- [ ] `NEXT_PUBLIC_TOLGEE_API_URL` يشير إلى سيرفر Tolgee الصحيح
- [ ] `NEXT_PUBLIC_TOLGEE_API_KEY` API key صحيح
- [ ] `NEXT_PUBLIC_DEFAULT_LOCALE` = `ar`
- [ ] `NEXT_PUBLIC_SUPPORTED_LOCALES` = `ar,en`

#### ✅ 2.3 الأمان
- [ ] `.env.local` في `.gitignore`
- [ ] `.env.example` محدّث بالمتغيرات (بدون قيم)
- [ ] لا تظهر API keys في Git history

#### 🧪 اختبار:
```bash
# يجب أن يطبع المتغيرات
node -e "console.log(process.env.NEXT_PUBLIC_TOLGEE_API_URL)"

# يجب ألا يكون في Git
git status | grep -q ".env.local" && echo "❌ موجود في Git!" || echo "✅ غير موجود في Git"
```

---

## 📁 3. بنية المجلدات

### المعايير:

#### ✅ 3.1 المجلدات موجودة
- [ ] `src/providers/i18n/` موجود
- [ ] `src/lib/i18n/` موجود
- [ ] `public/locales/ar/` موجود
- [ ] `public/locales/en/` موجود

#### 🧪 اختبار:
```bash
test -d src/providers/i18n && echo "✅" || echo "❌"
test -d src/lib/i18n && echo "✅" || echo "❌"
test -d public/locales/ar && echo "✅" || echo "❌"
test -d public/locales/en && echo "✅" || echo "❌"
```

---

## 📄 4. الملفات الأساسية

### المعايير:

#### ✅ 4.1 Constants file
- [ ] `src/lib/i18n/constants.ts` موجود
- [ ] يحتوي على:
  - `SUPPORTED_LOCALES`
  - `DEFAULT_LOCALE`
  - `FALLBACK_LOCALE`
  - `LOCALE_NAMES`
  - `RTL_LOCALES`
  - `NAMESPACES`

#### ✅ 4.2 Types file
- [ ] `src/lib/i18n/types.ts` موجود
- [ ] يحتوي على interfaces مطلوبة

#### ✅ 4.3 Fallback translations
- [ ] `public/locales/ar/common.json` موجود وصحيح
- [ ] `public/locales/en/common.json` موجود وصحيح
- [ ] يحتويان على نفس المفاتيح

#### 🧪 اختبار:
```bash
# تحقق من وجود الملفات
test -f src/lib/i18n/constants.ts && echo "✅ Constants" || echo "❌"
test -f src/lib/i18n/types.ts && echo "✅ Types" || echo "❌"
test -f public/locales/ar/common.json && echo "✅ AR common" || echo "❌"
test -f public/locales/en/common.json && echo "✅ EN common" || echo "❌"

# تحقق من صحة JSON
node -e "require('./public/locales/ar/common.json')" && echo "✅ Valid JSON"
```

---

## 🌐 5. اتصال Tolgee

### المعايير:

#### ✅ 5.1 الاتصال ناجح
- [ ] يمكن الوصول إلى Tolgee API
- [ ] Authentication ناجح مع API key
- [ ] يمكن جلب قائمة المشاريع

#### 🧪 اختبار:
```bash
# تنفيذ script الاختبار
npx tsx scripts/test-tolgee-connection.ts
```

**النتيجة المتوقعة**:
```
✅ الاتصال بـ Tolgee ناجح!
📊 عدد المشاريع: 1
```

#### ✅ 5.2 API Response
- [ ] Response code = 200
- [ ] Response يحتوي على بيانات صحيحة
- [ ] لا توجد CORS errors

---

## 🏗️ 6. Build وCompilation

### المعايير:

#### ✅ 6.1 TypeScript
- [ ] لا توجد Type errors
- [ ] `tsc --noEmit` ينجح

#### ✅ 6.2 Next.js Build
- [ ] `npm run build` ينجح بدون أخطاء
- [ ] Build size معقول (لم يزد كثيراً)

#### 🧪 اختبار:
```bash
# TypeScript check
npx tsc --noEmit

# Next.js build
npm run build
```

**النتيجة المتوقعة**: Build ناجح بدون أخطاء

---

## 📊 مصفوفة القبول الشاملة

| المعيار | الوزن | الحالة | ملاحظات |
|---------|-------|--------|----------|
| تثبيت المكتبات | عالي | ⏳ | - |
| متغيرات البيئة | عالي | ⏳ | - |
| بنية المجلدات | متوسط | ⏳ | - |
| الملفات الأساسية | عالي | ⏳ | - |
| اتصال Tolgee | حرج | ⏳ | يجب أن ينجح 100% |
| Build ناجح | عالي | ⏳ | - |

---

## ✅ شروط الانتقال للمرحلة 2

### يجب تحقيق:

1. **جميع** المعايير الحرجة (عالية الوزن) ✅
2. **90%** من المعايير المتوسطة ✅
3. Build ناجح بدون أخطاء ✅
4. اتصال Tolgee يعمل 100% ✅
5. مراجعة من Lead Developer ✅

---

## 📝 قائمة التحقق النهائية

قبل إغلاق المرحلة، تحقق من:

- [ ] جميع المكتبات مثبتة
- [ ] `.env.local` معد بشكل صحيح
- [ ] جميع المجلدات والملفات موجودة
- [ ] Tolgee API يعمل
- [ ] `npm run build` ناجح
- [ ] TypeScript بدون أخطاء
- [ ] `.env.local` في `.gitignore`
- [ ] Code مراجَع
- [ ] Documentation محدّث

---

## 🎯 نسبة الاكتمال

**الحالية**: 0%

**مطلوب للانتقال**: 100%

---

## 📅 التتبع

| التاريخ | الحالة | النسبة | الملاحظات |
|---------|--------|--------|-----------|
| - | ⏳ قيد الانتظار | 0% | لم تبدأ بعد |

---

**تم الإنشاء**: 19 نوفمبر 2025  
**آخر تحديث**: 19 نوفمبر 2025
